### Garmin golf data analytics.
Vibe coded app for personal analytics of exported data from garmin golf.

### Deployment:
`npm run deploy` pushes the build folder to `gh-pages` which triggers deployment to https://garmindata.com
